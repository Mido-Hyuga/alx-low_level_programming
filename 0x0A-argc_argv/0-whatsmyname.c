#include <stdio.h>
/**
*main - Print the name of main program, followed by a new line.
*@argc: Int
*@argv: String
*Return: return 0
*/
int main(int __attribute__((unused)) argc, char *argv[])
{
	printf("%s\n", argv[0]);
	return (0);
}
